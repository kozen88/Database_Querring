uso dei file presenti:

1. db_bancario.sql --> serve per la creazione del database banca. 
2. query_per_progetto_banca.sql --> serve per effettuare le query proposte ed estrarre la tabella che si vuole 				    ottenere.
3. tabella_denormalizzata.csv --> è il file che contiene l'esportazione della query finale che ci permette di 				  ottenere la tabella richiesta dal progetto.